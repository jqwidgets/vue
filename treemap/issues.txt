ALL
    -> source - Expected Object, got Array
        => normal type is an array

    -> there is no `getWidth("TreeMap")`

    -> legendScaleCallback - Expected Object, got Function.
import Vue from 'vue'
import App from './App'

Vue.config.productionTip = false

new Vue({
  el: '#app',
  components: { App },
  template: '<App/>'
})